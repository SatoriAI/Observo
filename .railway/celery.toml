[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = """
    sh -c '
        cd observo &&
        uv run --no-sync --no-dev celery -A observo.celery worker --loglevel=info --concurrency 2
    '
"""
